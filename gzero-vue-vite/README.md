# GZERO Vue + Vite

A responsive single-page Vue dashboard built as a static Vite application.

```bash
yarn install
yarn dev
```

For deployment, run `yarn build` and publish the generated `dist` directory.
