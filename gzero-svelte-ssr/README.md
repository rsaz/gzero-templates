# GZERO Svelte SSR

A single-page SvelteKit dashboard rendered on the server using adapter-node.

```bash
yarn install
yarn dev
```

Production: run `yarn build`, then `yarn start`. The server respects the `HOST` and `PORT` environment variables.
