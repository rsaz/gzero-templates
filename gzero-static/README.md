# GZERO Static

A zero-dependency, single-page dashboard made with plain HTML, CSS, and JavaScript.

```bash
yarn dev
```

For deployment, run `yarn build` and publish `dist`. To test the production output locally, run `yarn start`. The included server respects the `HOST` and `PORT` environment variables.
