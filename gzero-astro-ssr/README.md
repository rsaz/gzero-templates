# GZERO Astro SSR

A single-page Astro dashboard rendered at request time with the standalone Node adapter.

```bash
yarn install
yarn dev
```

Production: run `yarn build`, then `yarn start`. The server respects the `HOST` and `PORT` environment variables.
